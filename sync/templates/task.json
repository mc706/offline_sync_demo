{
    "uuid":"{{task.key}}",
    "title":"{{task.title}}",
    "completed":{%if task.completed%}true{%else%}false{%endif%},
    "deleted":{%if task.deleted%}true{%else%}false{%endif%},
    "timestamp":"{{task.date_modified|date:"U"}}"
}